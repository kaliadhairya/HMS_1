const test=require('node:test'); const assert=require('node:assert/strict'); const {loadConfig}=require('../src/config'); test('OPD config validates internal secret',()=>assert.throws(()=>loadConfig({INTERNAL_SERVICE_SECRET:'short'}),/at least 32/)); test('OPD config selects deterministic port',()=>assert.equal(loadConfig({INTERNAL_SERVICE_SECRET:'x'.repeat(32),OPD_PORT:'5103'}).port,5103));
