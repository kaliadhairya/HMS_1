const test=require('node:test'); const assert=require('node:assert/strict'); const pino=require('pino'); const {createApp}=require('../src/app'); const config={serviceName:'opd-service',internalServiceSecret:'x'.repeat(32),patientUrl:'http://127.0.0.1:1',identityUrl:'http://127.0.0.1:1',requestTimeoutMs:100}; const sequelize={authenticate:async()=>{}}; const models={}; test('OPD health is public',async()=>{const app=createApp(config,pino({enabled:false}),models,sequelize);const s=app.listen(0);await new Promise(r=>s.once('listening',r));const r=await fetch(`http://127.0.0.1:${s.address().port}/health`);assert.equal(r.status,200);s.close()}); test('OPD API requires gateway context',async()=>{const app=createApp(config,pino({enabled:false}),models,sequelize);const s=app.listen(0);await new Promise(r=>s.once('listening',r));const r=await fetch(`http://127.0.0.1:${s.address().port}/api/hms/departments`);assert.equal(r.status,401);s.close()});
